<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Jekyll v4.0.1">
    <title>ToDoList</title>
    <link rel="canonical" href="https://getbootstrap.com/docs/4.5/examples/checkout/">

    <!-- Bootstrap core CSS -->
<link href="assets/dist/css/bootstrap.css" rel="stylesheet">

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>
    <!-- Custom styles for this template -->
    <link href="css/index-page.css" rel="stylesheet">
  </head>
  <body class="bg-light">
    <div class="container">
  <div class="py-5 text-center">
    <img class="d-block mx-auto mb-4" src="assets/brand/ToDo-List-icon.png" alt="" width="96" height="96">
    <h2>Главная страница</h2>
    <p class="lead">Страница со списком категорий и задач</p>
  </div>

  <div class="row">
    <div class="col-md-4 order-md-2 mb-4">
      <h4 class="d-flex justify-content-between align-items-center mb-3">
        <span class="text-muted">Категории</span>
        <span class="badge badge-secondary badge-pill">{{countCategory}}</span>
      </h4>
      <ul class="list-group mb-3">
          {{#categories}}
        <li class="list-group-item d-flex justify-content-between">
          <span>{{name}}</span>
          <strong></strong>
        </li>
        {{/categories}}
      </ul>

      <form class="card p-2">
        <div class="input-group">
          <a href="/category" type="submit" class="btn btn-primary btn-block">Добавить категорию</a>
        </div>
      </form>
    </div>
    <div class="col-md-8 order-md-1">
      <div class="row">
        <div class="col-sm-8">
          <h4 class="mb-3">Задачи</h4>
        </div>
        <div class="col-sm-4">
          <a href="/task" type="submit" class="btn btn-primary btn-block">Добавить задачу </a>
        </div>
      </div>
      <table class="table table-hover">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Название</th>
            <th scope="col">Дата начала</th>
            <th scope="col">Статус</th>
            <th scope="col">Дата завершения</th>
            <th scope="col">Ред.</th>
            <th scope="col">Уд.</th>
          </tr>
        </thead>
        <tbody>
          {{#tasks}}
              <tr>
                  <th scope="row">{{id}}</th>
                  <td>{{name}}</td>
                  <td>{{dateStart}}</td>
                  <td>@{{status}}</td>
                  <td>{{dateEnd}}</td>
                  <td><a href="task/update/{{id}}"><img class="svg-edit" src="/assets/brand/edit-icon.svg"/></a></td>
                  <td><a href="task/delete/{{id}}"><img class="svg-delete" src="/assets/brand/delete-icon.svg"/></a></td>
              </tr>
          {{/tasks}}
        </tbody>
      </table>
    </div>
  </div>

  <footer class="my-5 pt-5 text-muted text-center text-small">
    <p class="mb-1">&copy; 2020 ToDoList</p>
  </footer>
</div>
</body>
</html>
